{
  "name": "my-angular-project-x",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "if [ $(which nodemon)  ]; then nodemon -w server.js server.js; else node server.js; fi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "express-stormpath": "^2.0.5"
  }
}
